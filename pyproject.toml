[project]
name = "pid-register"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.28.1",
    "kurra>=3.0.6",
    "rdflib>=7.1.4",
]

[dependency-groups]
dev = [
    "pytest>=8.3.5",
]
